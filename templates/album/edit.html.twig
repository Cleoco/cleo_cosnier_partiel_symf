{% extends 'base.html.twig' %}

{% block title %}Edit Album{% endblock %}

{% block body %}
<div class="container">
    <h1>Modifier un album</h1>
    <div class="filet"></div>
    <div class="btn_action">
        <a class=" btn btn-primary"href="{{ path('album_index') }}">Retourner aux albums</a>
    </div>

    {{ include('album/_form.html.twig', {'button_label': 'Mettre à jour'},{'class':'btn btn-success'}) }}
    {{ include('album/_delete_form.html.twig') }}


</div>
{% endblock %}
